services:
  - type: web
    name: zilantia-arcanes
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python3 database_setup.py"
    startCommand: "python3 arcanes_bot.py"
